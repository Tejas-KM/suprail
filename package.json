{
  "main": "backend/server.js",
  "dependencies": {
    "adm-zip": "^0.5.16",
    "axios": "^1.12.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-icons": "^5.5.0",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "npm --prefix frontend ci --no-audit --progress=false && npm --prefix frontend run build",
    "build": "npm --prefix frontend run build"
  }
}
