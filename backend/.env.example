# Backend environment variables
PORT=5000
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority

# Publicly accessible base URL for building absolute image links for legacy images
# BACKEND_URL=https://apptest-ngjs.onrender.com

# CORS allow-list (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,https://apptest-xplp.vercel.app

# Supabase configuration (Storage + optional Postgres)
SUPABASE_URL=https://rbgnwhbtntmnilkxpqcf.supabase.co
# Prefer a service role key on the server for full Storage control. If unavailable, the anon key works for uploads to public buckets.
SUPABASE_KEY=
# or
# SUPABASE_SERVICE_ROLE_KEY=
# or
# SUPABASE_ANON_KEY=
SUPABASE_BUCKET=uploads

# Optional: Supabase Postgres connection string (not used directly unless you switch from MongoDB)
SUPABASE_DB_URL=postgresql://postgres:19tejas4@db.rbgnwhbtntmnilkxpqcf.supabase.co:5432/postgres

# Secure report download API
# Set a strong key and use the secure endpoint:
#   GET /api/projects/download/by-date-secure?from=YYYY-MM-DD&to=YYYY-MM-DD&key=YOUR_KEY
REPORT_API_KEY=
# Optional: comma-separated list of allowed IPs (e.g., "1.2.3.4,5.6.7.8"). Leave empty to allow any IP with a valid key.
REPORT_ALLOWED_IPS=
